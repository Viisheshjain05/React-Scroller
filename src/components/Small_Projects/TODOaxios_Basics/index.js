//! FILE TO SHOW INDEX FILE MODIFICATIONS ONLY

// import React from "react";
// import ReactDOM from "react-dom";
// import App from "./App";
// import Axios from "axios";

// ? Add baseURL in Default for all type of requests and other parameter may be needed
// Axios.defaults.baseURL = "https://jsonplaceholder.typicode.com";
// Axios.defaults.headers.common["AUTHORIZATION"] = "auth Token";

// ? Use to Config Headers And req res configs
// Axios.interceptors.request.use(
//   (req) => {
//     console.log(req);
//     //! Edit req before sending
//     return req;
//   },
//   (err) => {
//     console.log(err);
    // ! In case To handle error globaly also return error to handle it in local files
//     return Promise.reject(err);
//   }
// );

// ReactDOM.render(<App />, document.getElementById("root"));
